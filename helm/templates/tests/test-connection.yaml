apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oasbinder.fullname" . }}-test-connection"
  labels:
    {{- include "oasbinder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oasbinder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
